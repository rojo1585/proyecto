
/*
 // funciones como parametro
// creando la funcion 
function greet() {
    console.log(`hi`);
}
greet();

//una funcion que recibe otra funcion de parametro

function logGreeting(fn) {
    fn();
}
logGreeting(greet);
*/

/*
//funcion de flecha
let greet = () => {
    console.log(`HI`);
}

greet();

let loggeGreating = (fn) => { fn(); }
   
loggeGreating(greet);
*/

// funcion de fleja con una funcion anonima
//funcion expression on de fly(funcion que se declara al momento)
/*
let greet = () => { console.log(`HI`);}

greet();

let loggeGreating = (fn) => { fn(); }

loggeGreating(() => { console.log(`HI`); })
*/

